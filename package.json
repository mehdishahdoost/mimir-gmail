{
  "name": "mimir-gmail",
  "version": "1.0.3",
  "description": "MCP server for Gmail - read, send, search, and manage emails via AI assistants",
  "main": "dist/index.js",
  "bin": {
    "mimir-gmail": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "npm run build && node --test test/*.test.cjs",
    "start": "node dist/index.js",
    "auth": "node dist/cli.js auth",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "mcp",
    "gmail",
    "email",
    "model-context-protocol",
    "ai",
    "claude"
  ],
  "author": "",
  "files": [
    "dist",
    "assets/logo.png"
  ],
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "googleapis": "^148.0.0",
    "google-auth-library": "^9.15.1",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
